# TrajPlayer v0.1.0-alpha.2

This prerelease fixes Windows cache-lock failures without changing TrajPlayer's
visual appearance, rendering quality, or playback behavior.

## Fixed

- Rapid consecutive Open or drag-and-drop requests now wait for the previous
  background cache worker to stop before starting the next request.
- A cache held open by another Windows process no longer causes WinError 32;
  TrajPlayer uses an isolated session cache and removes it on close.
- Conda-based Windows packaging now includes required runtime DLLs even when the
  environment was invoked directly instead of activated first.

## Windows

Download `TrajPlayer-Windows-x64-v0.1.0-alpha.2.zip` from the assets below, not
the source archive under **Code > Download ZIP**. Extract the complete archive
and run `TrajPlayer\TrajPlayer.exe`. Python and Conda are not required. Keep
`_internal` beside the executable; do not copy or share the EXE by itself.

## Linux

Download and completely extract
`TrajPlayer-Linux-x86_64-v0.1.0-alpha.2.tar.gz`, then run:

```bash
chmod +x TrajPlayer/TrajPlayer
./TrajPlayer/TrajPlayer
```

Python is not required. The portable build requires Linux x86_64, OpenGL 3.3,
and common Qt XCB runtime libraries.

## Opening Trajectories

Use **Open** or drag files into the application. ASE `.traj`, `.xyz`, and
`.extxyz` files open directly. For Gromacs, select or drag a GRO topology
together with its XTC/TRR trajectory. TrajPlayer can also try a same-named GRO
beside an XTC/TRR file when the trajectory is opened by itself.

This is an alpha release. Cache metadata and file compatibility may change;
keep original trajectories and report reproducible failures through GitHub
Issues.
